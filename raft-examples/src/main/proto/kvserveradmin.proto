// Copyright 2024-2026 The x-raft-lib Authors
//
// Licensed under the Apache License, Version 2.0 (the "License").
// SPDX-License-Identifier: Apache-2.0

syntax = "proto3";

package kvserveradmin;

option java_package = "io.github.xinfra.lab.raft.examples.proto";
option java_outer_classname = "KvServerAdminProto";
option java_multiple_files = true;

service KvAdminService {
    rpc AddNode(AddNodeRequest) returns (AddNodeResponse);
    rpc RemoveNode(RemoveNodeRequest) returns (RemoveNodeResponse);
    rpc ReplaceNode(ReplaceNodeRequest) returns (ReplaceNodeResponse);
    rpc TransferLeader(TransferLeaderRequest) returns (TransferLeaderResponse);
    rpc GetClusterInfo(GetClusterInfoRequest) returns (GetClusterInfoResponse);
}

message AddNodeRequest {
    uint64 node_id = 1;
    string address = 2;
    bool is_learner = 3;
}

message AddNodeResponse {}

message RemoveNodeRequest {
    uint64 node_id = 1;
}

message RemoveNodeResponse {}

// Atomically removes one node and adds another in a single conf change.
message ReplaceNodeRequest {
    uint64 remove_node_id = 1;
    uint64 add_node_id = 2;
    string add_address = 3;
}

message ReplaceNodeResponse {}

message TransferLeaderRequest {
    uint64 transferee = 1;
}

message TransferLeaderResponse {}

message GetClusterInfoRequest {}

message GetClusterInfoResponse {
    uint64 leader_id = 1;
    uint64 node_id = 2;
    string state = 3;
    uint64 term = 4;
    uint64 commit = 5;
    uint64 applied = 6;
    repeated uint64 voters = 7;
    repeated uint64 learners = 8;
}
